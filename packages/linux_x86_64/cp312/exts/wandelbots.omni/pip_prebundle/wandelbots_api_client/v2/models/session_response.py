# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List
from wandelbots_api_client.v2.models.capability_entry import CapabilityEntry
from wandelbots_api_client.v2.models.user import User
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class SessionResponse(BaseModel):
    """
    Session information including user identity, capabilities, and token lifetime metadata. On unmanaged instances (no authentication or authorization configured), a default anonymous session is returned with `session_id` \"default\", empty user fields, no capabilities, and zero-value timestamps.
    """  # noqa: E501

    session_id: StrictStr = Field(
        description='Unique identifier for the current session. For authenticated sessions this is derived from the access token. Unmanaged instances return "default".'
    )
    user: User
    capabilities: List[CapabilityEntry] = Field(description="Capability entries resolved for the user. Empty on unmanaged instances.")
    issued_at: datetime = Field(description="Timestamp when the underlying access token was issued. Zero-value on unmanaged instances.")
    expires_at: datetime = Field(description="Timestamp when the underlying access token expires. Zero-value on unmanaged instances.")
    __properties: ClassVar[List[str]] = ["session_id", "user", "capabilities", "issued_at", "expires_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SessionResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of user
        if self.user:
            _dict["user"] = self.user.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in capabilities (list)
        _items = []
        if self.capabilities:
            for _item_capabilities in self.capabilities:
                if _item_capabilities:
                    _items.append(_item_capabilities.to_dict())
            _dict["capabilities"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SessionResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "session_id": obj.get("session_id"),
                "user": User.from_dict(obj["user"]) if obj.get("user") is not None else None,
                "capabilities": [CapabilityEntry.from_dict(_item) for _item in obj["capabilities"]]
                if obj.get("capabilities") is not None
                else None,
                "issued_at": obj.get("issued_at"),
                "expires_at": obj.get("expires_at"),
            }
        )
        return _obj
