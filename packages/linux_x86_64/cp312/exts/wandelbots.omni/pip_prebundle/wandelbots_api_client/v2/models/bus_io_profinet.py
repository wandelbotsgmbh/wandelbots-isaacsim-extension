# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from wandelbots_api_client.v2.models.bus_io_profinet_default_route import BusIOProfinetDefaultRoute
from wandelbots_api_client.v2.models.bus_io_profinet_network import BusIOProfinetNetwork
from wandelbots_api_client.v2.models.bus_io_profinet_slot import BusIOProfinetSlot
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class BusIOProfinet(BaseModel):
    """
    PROFINET BUS inputs/outputs service configuration.
    """  # noqa: E501

    bus_type: StrictStr
    network_config: Optional[BusIOProfinetNetwork] = Field(
        default=None,
        description="Used to enable IP communication through the same physical ethernet interface while PROFINET driver is active.  Optional for SR-IOV-based bus I/O recreation. If omitted and an existing PROFINET bus I/O configuration for the same MAC address already exists, the previous network settings are reused. ",
    )
    plc_ip: StrictStr = Field(
        description="IP address of the PLC to establish PROFINET communication with.  The IP address is used to configure the host machine's firewall. The firewall configuration is applied while the BUS input/output service is active. Without proper firewall configuration, PROFINET device discovery, configuration exchanges, and real-time cyclic data communication will fail. "
    )
    mac: StrictStr = Field(description="MAC address of the physical ethernet interface that you want to use for PROFINET communication. ")
    slots: Optional[Annotated[List[BusIOProfinetSlot], Field(min_length=1, max_length=12)]] = Field(
        default=None,
        description="Optional PROFINET slot configuration.  If not provided, the default configuration is used for backward compatibility: one slot with a size of 64 bytes. ",
    )
    default_route: Optional[BusIOProfinetDefaultRoute] = None
    __properties: ClassVar[List[str]] = ["bus_type", "network_config", "plc_ip", "mac", "slots", "default_route"]

    @field_validator("bus_type")
    def bus_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["profinet"]):
            raise ValueError("must be one of enum values ('profinet')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BusIOProfinet from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of network_config
        if self.network_config:
            _dict["network_config"] = self.network_config.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in slots (list)
        _items = []
        if self.slots:
            for _item_slots in self.slots:
                if _item_slots:
                    _items.append(_item_slots.to_dict())
            _dict["slots"] = _items
        # override the default output from pydantic by calling `to_dict()` of default_route
        if self.default_route:
            _dict["default_route"] = self.default_route.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BusIOProfinet from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "bus_type": obj.get("bus_type"),
                "network_config": BusIOProfinetNetwork.from_dict(obj["network_config"]) if obj.get("network_config") is not None else None,
                "plc_ip": obj.get("plc_ip"),
                "mac": obj.get("mac"),
                "slots": [BusIOProfinetSlot.from_dict(_item) for _item in obj["slots"]] if obj.get("slots") is not None else None,
                "default_route": BusIOProfinetDefaultRoute.from_dict(obj["default_route"])
                if obj.get("default_route") is not None
                else None,
            }
        )
        return _obj
