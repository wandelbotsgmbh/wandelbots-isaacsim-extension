# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from wandelbots_api_client.v2.models.collider_value_or_key import ColliderValueOrKey
from wandelbots_api_client.v2.models.link_chain_value_or_key import LinkChainValueOrKey
from wandelbots_api_client.v2.models.tool_value_or_key import ToolValueOrKey
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class CollisionSetupValue(BaseModel):
    """
    An inline collision setup wrapped for discriminator support. Colliders, tool, and link_chain can each be either inline values or storage key references.
    """  # noqa: E501

    source: StrictStr = Field(description="Discriminator value indicating an inline value.")
    colliders: Optional[Dict[str, ColliderValueOrKey]] = Field(
        default=None, description="Colliders checked against links and tool. Each value is either an inline Collider or a store key. "
    )
    link_chain: Optional[LinkChainValueOrKey] = Field(
        default=None, description="The shape of the motion group links. Either an inline LinkChain or a store key. "
    )
    tool: Optional[ToolValueOrKey] = Field(default=None, description="Shape of the tool. Either an inline tool collider or a store key. ")
    self_collision_detection: Optional[StrictBool] = Field(
        default=True, description="If true, self-collision detection is enabled. Default is true. "
    )
    __properties: ClassVar[List[str]] = ["source", "colliders", "link_chain", "tool", "self_collision_detection"]

    @field_validator("source")
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["value"]):
            raise ValueError("must be one of enum values ('value')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CollisionSetupValue from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in colliders (dict)
        _field_dict = {}
        if self.colliders:
            for _key_colliders in self.colliders:
                if self.colliders[_key_colliders]:
                    _field_dict[_key_colliders] = self.colliders[_key_colliders].to_dict()
            _dict["colliders"] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of link_chain
        if self.link_chain:
            _dict["link_chain"] = self.link_chain.to_dict()
        # override the default output from pydantic by calling `to_dict()` of tool
        if self.tool:
            _dict["tool"] = self.tool.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CollisionSetupValue from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "source": obj.get("source"),
                "colliders": dict((_k, ColliderValueOrKey.from_dict(_v)) for _k, _v in obj["colliders"].items())
                if obj.get("colliders") is not None
                else None,
                "link_chain": LinkChainValueOrKey.from_dict(obj["link_chain"]) if obj.get("link_chain") is not None else None,
                "tool": ToolValueOrKey.from_dict(obj["tool"]) if obj.get("tool") is not None else None,
                "self_collision_detection": obj.get("self_collision_detection")
                if obj.get("self_collision_detection") is not None
                else True,
            }
        )
        return _obj
