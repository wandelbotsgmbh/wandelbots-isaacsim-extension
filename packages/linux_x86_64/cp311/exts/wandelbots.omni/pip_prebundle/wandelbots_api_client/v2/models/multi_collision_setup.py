# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from wandelbots_api_client.v2.models.collider import Collider
from wandelbots_api_client.v2.models.collision_motion_group import CollisionMotionGroup
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class MultiCollisionSetup(BaseModel):
    """
    MultiCollisionSetup
    """  # noqa: E501

    collision_motion_groups_by_motion_group_key: Optional[Dict[str, CollisionMotionGroup]] = Field(
        default=None, description="Dictionary of motion group collision motion groups containing link chain and tool colliders. "
    )
    colliders: Optional[Dict[str, Collider]] = Field(default=None, description="Colliders are checked against links and tool. ")
    __properties: ClassVar[List[str]] = ["collision_motion_groups_by_motion_group_key", "colliders"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MultiCollisionSetup from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in collision_motion_groups_by_motion_group_key (dict)
        _field_dict = {}
        if self.collision_motion_groups_by_motion_group_key:
            for _key_collision_motion_groups_by_motion_group_key in self.collision_motion_groups_by_motion_group_key:
                if self.collision_motion_groups_by_motion_group_key[_key_collision_motion_groups_by_motion_group_key]:
                    _field_dict[_key_collision_motion_groups_by_motion_group_key] = self.collision_motion_groups_by_motion_group_key[
                        _key_collision_motion_groups_by_motion_group_key
                    ].to_dict()
            _dict["collision_motion_groups_by_motion_group_key"] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of each value in colliders (dict)
        _field_dict = {}
        if self.colliders:
            for _key_colliders in self.colliders:
                if self.colliders[_key_colliders]:
                    _field_dict[_key_colliders] = self.colliders[_key_colliders].to_dict()
            _dict["colliders"] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MultiCollisionSetup from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "collision_motion_groups_by_motion_group_key": dict(
                    (_k, CollisionMotionGroup.from_dict(_v)) for _k, _v in obj["collision_motion_groups_by_motion_group_key"].items()
                )
                if obj.get("collision_motion_groups_by_motion_group_key") is not None
                else None,
                "colliders": dict((_k, Collider.from_dict(_v)) for _k, _v in obj["colliders"].items())
                if obj.get("colliders") is not None
                else None,
            }
        )
        return _obj
