# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool
from typing import Any, ClassVar, Dict, List, Optional
from wandelbots_api_client.v2.models.collider import Collider
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class CollisionMotionGroup(BaseModel):
    """
    CollisionMotionGroup
    """  # noqa: E501

    link_chain: Optional[List[Dict[str, Collider]]] = Field(
        default=None,
        description="The shape of the motion groups links to validate against colliders. Indexed along the kinematic chain, starting with a static base shape before first joint. The base of the motion group is not checked for collision against the environment. ",
    )
    tool: Optional[Dict[str, Collider]] = Field(default=None, description="Shape of the tool to validate against colliders. ")
    self_collision_detection: Optional[StrictBool] = Field(
        default=True,
        description="If true, self-collision detection is enabled for the motion group.  Self-collision detection checks if links in the kinematic chain of the motion group collide with each other. Adjacent links in the kinematic chain of the motion group are not checked for mutual collision. The tool is treated like a link at the end of the kinematic chain. It is checked against all links except the last one.  Default is true. ",
    )
    __properties: ClassVar[List[str]] = ["link_chain", "tool", "self_collision_detection"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CollisionMotionGroup from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in link_chain (list of dict)
        _items = []
        if self.link_chain:
            for _item_link_chain in self.link_chain:
                if _item_link_chain:
                    _items.append({_inner_key: _inner_value.to_dict() for _inner_key, _inner_value in _item_link_chain.items()})
            _dict["link_chain"] = _items
        # override the default output from pydantic by calling `to_dict()` of each value in tool (dict)
        _field_dict = {}
        if self.tool:
            for _key_tool in self.tool:
                if self.tool[_key_tool]:
                    _field_dict[_key_tool] = self.tool[_key_tool].to_dict()
            _dict["tool"] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CollisionMotionGroup from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "link_chain": [
                    {_inner_key: Collider.from_dict(_inner_value) for _inner_key, _inner_value in _item.items()}
                    for _item in obj["link_chain"]
                ]
                if obj.get("link_chain") is not None
                else None,
                "tool": dict((_k, Collider.from_dict(_v)) for _k, _v in obj["tool"].items()) if obj.get("tool") is not None else None,
                "self_collision_detection": obj.get("self_collision_detection")
                if obj.get("self_collision_detection") is not None
                else True,
            }
        )
        return _obj
