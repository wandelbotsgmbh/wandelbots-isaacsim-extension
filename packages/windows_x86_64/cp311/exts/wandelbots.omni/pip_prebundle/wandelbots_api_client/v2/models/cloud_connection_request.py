# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class CloudConnectionRequest(BaseModel):
    """
    CloudConnectionRequest
    """  # noqa: E501

    cloud_hostname: StrictStr
    token: StrictStr = Field(
        description="Auth token of the user registering the instance. This must be a valid oauth token granting access to the NOVA Cloud API."
    )
    instance_name: StrictStr = Field(description="Human-readable name to identify the instance in the fleet manager dashboard.")
    instance_url: Optional[StrictStr] = Field(
        default=None,
        description="Optional local network url for the instance, used to link users directly from the fleet manager. If not specified, will be inferred from the request URL.",
    )
    __properties: ClassVar[List[str]] = ["cloud_hostname", "token", "instance_name", "instance_url"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CloudConnectionRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CloudConnectionRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "cloud_hostname": obj.get("cloud_hostname"),
                "token": obj.get("token"),
                "instance_name": obj.get("instance_name"),
                "instance_url": obj.get("instance_url"),
            }
        )
        return _obj
