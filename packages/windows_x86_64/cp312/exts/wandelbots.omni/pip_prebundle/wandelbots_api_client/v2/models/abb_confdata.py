# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class AbbConfdata(BaseModel):
    """
    AbbConfdata
    """  # noqa: E501

    cf1: StrictInt = Field(
        description="Quadrant number for axis 1. Quadrant n means the axis angle is in the interval [n*90deg, (n+1)*90deg). Can be negative. "
    )
    cf4: StrictInt = Field(
        description="Quadrant number for axis 4. Quadrant n means the axis angle is in the interval [n*90deg, (n+1)*90deg). Can be negative. "
    )
    cf6: StrictInt = Field(
        description="Quadrant number for axis 6. Quadrant n means the axis angle is in the interval [n*90deg, (n+1)*90deg). Can be negative. "
    )
    cfx: Annotated[int, Field(le=7, strict=True, ge=0)] = Field(
        description="3 branch bits composing the kinematic configuration: - Bit 0 (Wrist): 0 = positive axis 5 angle `NO_FLIP`, 1 = negative `FLIP`. - Bit 1 (Elbow): 0 = in front of lower arm `DOWN`, 1 = behind `UP`. - Bit 2 (Shoulder): 0 = in front of axis 1 `LEFT`, 1 = behind `RIGHT`. "
    )
    __properties: ClassVar[List[str]] = ["cf1", "cf4", "cf6", "cfx"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AbbConfdata from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AbbConfdata from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({"cf1": obj.get("cf1"), "cf4": obj.get("cf4"), "cf6": obj.get("cf6"), "cfx": obj.get("cfx")})
        return _obj
