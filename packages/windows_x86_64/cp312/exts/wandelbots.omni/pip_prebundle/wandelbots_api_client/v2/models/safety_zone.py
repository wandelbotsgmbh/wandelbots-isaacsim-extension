# coding: utf-8

"""
Wandelbots NOVA API

Interact with robots in an easy and intuitive way.

The version of the OpenAPI document: 2.6.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from wandelbots_api_client.v2.models.safety_geometry import SafetyGeometry
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


class SafetyZone(BaseModel):
    """
    Defines a safety zone with geometric boundaries and constraints. Safety zones are used to restrict robot motion or define protected areas.
    """  # noqa: E501

    id: StrictInt = Field(description="Unique identifier of the safety zone.")
    name: StrictStr = Field(description="Name of the safety zone.")
    priority: Optional[StrictInt] = Field(default=0, description="Priority of the safety zone.")
    restricted: Optional[StrictBool] = Field(default=False, description="If `true`, the zone is restricted and the robot cannot enter it.")
    geometry: SafetyGeometry = Field(description="The geometric shape of the safety zone.")
    mg_uid: Optional[StrictInt] = Field(
        default=None,
        description="UID of the motion group this safety zone is attached to. Must match an existing motion group UID on the controller. ",
    )
    uid_ref_cs: Optional[StrictStr] = Field(
        default=None,
        description='UID of the coordinate system in which the geometry coordinates are expressed. The coordinate system must be defined on the controller beforehand. If empty (""), the World coordinate system is used. ',
    )
    active: Optional[StrictBool] = Field(default=True, description="If `true`, the safety zone is active.")
    __properties: ClassVar[List[str]] = ["id", "name", "priority", "restricted", "geometry", "mg_uid", "uid_ref_cs", "active"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SafetyZone from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of geometry
        if self.geometry:
            _dict["geometry"] = self.geometry.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SafetyZone from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "id": obj.get("id"),
                "name": obj.get("name"),
                "priority": obj.get("priority") if obj.get("priority") is not None else 0,
                "restricted": obj.get("restricted") if obj.get("restricted") is not None else False,
                "geometry": SafetyGeometry.from_dict(obj["geometry"]) if obj.get("geometry") is not None else None,
                "mg_uid": obj.get("mg_uid"),
                "uid_ref_cs": obj.get("uid_ref_cs"),
                "active": obj.get("active") if obj.get("active") is not None else True,
            }
        )
        return _obj
